# Caddyfile
# Use the domain name passed from docker-compose environment
{$DOMAIN_NAME} {
    # Enable compression
    encode zstd gzip

    # Proxy all requests to the client service
    # Next.js will handle API routing via rewrites
    reverse_proxy client:3002
}
# Caddyfile
{$DOMAIN_NAME} {
    # Enable compression
    encode zstd gzip

    handle /api/* {
        max_size 10MB
        reverse_proxy backend:3001
    }

    # Proxy all other requests to the client service
    handle {
        reverse_proxy client:3002
    }
}